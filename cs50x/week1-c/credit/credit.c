#include <cs50.h>
#include <stdio.h>

int main(void)
{
    // Define the result var
    string result;

    // Ask for a long
    long long user_card = get_long_long("Number: ");

    // Verify a long
    // Do the checksum
    // Do the card company
    // Get the result

    // Print result of the long
    printf("%lli\n", user_card);
}